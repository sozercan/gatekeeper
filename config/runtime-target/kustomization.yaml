apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../default

patchesJson6902:
- target:
    group: apps
    version: v1
    kind: Deployment
    name: controller-manager
  path: manager_args_patch.yaml
- target:
    group: apps
    version: v1
    kind: Deployment
    name: audit
  path: audit_args_patch.yaml
- target:
    group: rbac.authorization.k8s.io
    version: v1
    kind: ClusterRole
    name: manager-role
  path: manager_rbac_patch.yaml
